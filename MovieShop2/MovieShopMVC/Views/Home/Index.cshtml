@model List<ApplicationCore.Models.MovieCard>


@* <h1>@ViewBag.Title
    ViewBag.Title
</h1>

@if (true)
{
    <div>Inside if</div>
} else
{
    <div>Inside else</div>
} *@

@foreach (var movie in Model)
{
    <div>@movie.Id</div>   
    <div>@movie.Title</div>
    <img src="@movie.PosterUrl" alt="@movie.Title" />
}